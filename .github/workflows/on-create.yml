name: Initialize New Lion

on:
  # Trigger when repository is created from template (Actions enabled automatically!)
  create:
  # Trigger when repository is forked (user must enable Actions manually)
  fork:
  # Also allow manual trigger
  workflow_dispatch:

permissions:
  contents: write
  issues: write

jobs:
  initialize_child:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
      
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      
      - name: Install dependencies
        run: |
          pip install -r requirements.txt
      
      - name: Initialize child lion from parent
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITHUB_REPOSITORY: ${{ github.repository }}
        run: |
          echo "🐵 New lion detected! Initializing..."
          python src/cli.py init --from-fork
      
      - name: Update README
        run: |
          echo "📝 Updating README..."
          python src/cli.py update-readme
      
      - name: Commit initial lion
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          
          git add lion_data/ README.md
          git commit -m "🐵 Initialize lion" || echo "No changes to commit"
          git push
      
      - name: Show new lion
        run: |
          echo "👶 Your new lion:"
          python src/cli.py show
      
      - name: Create welcome issue
        continue-on-error: true  # Forked repos may have issues disabled
        uses: actions/github-script@v7
        with:
          script: |
            const body = `## 🐵 Your Lion is Born!
            
            Congratulations! You've created your own ForkLion.
            
            ### What happens now?
            
            1. **Daily Evolution**: Your lion evolves automatically every day at midnight UTC
            2. **GitHub Pages**: View your lion at \`https://${context.repo.owner}.github.io/${context.repo.repo}/\`
            3. **AI-Powered**: Add ANTHROPIC_API_KEY secret for intelligent evolution
            
            ### Setup (Optional)
            
            1. Go to **Settings → Secrets → Actions**
            2. Add secret: \`ANTHROPIC_API_KEY\` (from https://console.anthropic.com)
            
            > **📢 Keep this repo PUBLIC** for GitHub Pages to work and to share your lion with the world!
            
            ### Your Lion
            
            Check the README to see your lion's current appearance and traits!
            
            Happy lion growing! 🌱`;
            
            github.rest.issues.create({
              owner: context.repo.owner,
              repo: context.repo.repo,
              title: '🎉 Welcome to ForkLion!',
              body: body
            });

